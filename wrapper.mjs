// eslint-disable-next-line import/extensions
import ephemeral from './src/index.js';

export const {
  CodeRegistry,
  ExpirationTime,
  RenewableCodeRegistry,
  jsonSerializer,
  generateCode,
} = ephemeral;
